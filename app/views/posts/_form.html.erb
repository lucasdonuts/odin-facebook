<%= form_with(model: post) do |f| %>
  <% if post.errors.any? %>
    <div class="notification is-global-notification is-danger is-light" id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="control">
      <%= f.text_area :body, autofocus: true, class: "textarea is-info has-fixed-size" %>
    </div>
  </div>

  <br>
  
  
  <div class="field is-grouped is-grouped-centered">
    <div class="control">
      <p class="title is-5">Add a photo?</p>
    </div>
    <div class="control">
      <%= image_tag(post.photo, width: 200) if post.photo.attached? %>
      <%= f.file_field :photo %>
    </div>
  </div>
  <div class="field is-grouped is-grouped-centered">
    <div class="control">
      <%= f.submit class: "button is-info" %>
    </div>
    <div class="control">
      <%= link_to "Cancel", root_path, class: "button is-info is-outlined" %>
    </div>
  </div>

<% end %>
